[project]
name = "sevaq-public"
version = "0.1.0"
description = "Public-safe orchestration and observability layer for SevaQ"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
